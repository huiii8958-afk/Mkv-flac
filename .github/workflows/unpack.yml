name: Unpack ZIP

on:
  workflow_dispatch:

jobs:
  unpack:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Extract ZIP
        run: |
          ZIP=$(find . -maxdepth 1 -name "*.zip" | head -1)
          unzip -o "$ZIP"
          rm "$ZIP"

      - name: Remove existing .github
        run: rm -rf .github || true

      - name: Move files
        run: |
          if [ -d mkv-flac-player ]; then
            shopt -s dotglob
            mv mkv-flac-player/* .
          fi

      - name: Commit
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Unpack project" || echo "No changes"
          git push